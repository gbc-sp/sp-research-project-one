import React, { Component } from "react";

class CompSelection extends Component {
  render() {
    return (
      <div>
        {/* put the HTML in between these divs */}
        <p>This is going to be the competency selection sidebar</p>
      </div>
    );
  }
}

export default CompSelection;
